export class Acceleration {
    increaseX: number;
    increaseY: number;
    increaseZ: number;


    constructor(increaseX: number, increaseY: number, increaseZ: number) {
        this.increaseX = increaseX;
        this.increaseY = increaseY;
        this.increaseZ = increaseZ;
    }
}
